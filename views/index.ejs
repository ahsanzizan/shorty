<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shorty</title>
    <link href="/output.css" rel="stylesheet" type="text/css">
</head>
<body>
    <div class="container mx-auto">
        <h1 class="font-bold mt-5 text-3xl uppercase">Shorty</h1>
        <form action="/shortUrls" method="POST" class="my-12">
            <div class="md:w-1/3">
                <label class=" text-gray-500 font-bold md:text-right mb-1 md:mb-0 pr-4" for="fullUrl">
                  Full URL
                </label>
            </div>
            <div class="md:w-2/3 mb-5">
                <input required class="bg-gray-200 appearance-none border-2 border-gray-200 rounded w-full py-2 px-4 text-gray-700 leading-tight focus:outline-none focus:bg-white focus:border-gray-800" id="fullUrl" type="url" placeholder="https://..." name="fullUrl">
            </div>
            <button type="submit" class="bg-gray-700 p-3 font-semibold text-white rounded-tl-lg rounded-br-lg hover:shadow-xl">Shorten</button>
        </form>
        
        <div class="overflow-x-auto relative">
            <table class="w-full text-sm text-left text-gray-500 dark:text-gray-400">
                <thead class="text-xs text-gray-700 uppercase bg-gray-50 dark:bg-gray-700 dark:text-gray-400">
                    <tr>
                        <th class="py-3 px-6">Full URL</th>
                        <th class="py-3 px-6">Shortened ID</th>
                        <th class="py-3 px-6">Clicks</th>
                    </tr>
                </thead>
                <tbody>
                    <% shortUrls.forEach(shortUrl => { %>
                        <tr class="bg-white border-b dark:bg-gray-800 dark:border-gray-700 font-semibold text-base">
                            <td class="py-4 px-6"><a href="<%= shortUrl.full %>"><%= shortUrl.full %></a></td>
                            <td class="py-4 px-6"><a href="<%= shortUrl.short %>"><%= fullUrl + shortUrl.short %></a></td>
                            <td class="py-4 px-6"><%= shortUrl.clicks %> </td>
                        </tr>
                    <% }) %>  
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>
